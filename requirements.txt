fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# LLM (Qwen via OpenAI-compatible API)
openai==1.51.0

# Vector database (already-populated collection is only loaded, not created)
chromadb>=0.5.6,<0.6.0

# LangChain (vectorstore wrapper + embeddings integration)
langchain==0.3.1
langchain-community==0.3.1
langchain-chroma==0.1.4
langchain-huggingface==0.1.0

# Embeddings model runtime
sentence-transformers==3.1.1
